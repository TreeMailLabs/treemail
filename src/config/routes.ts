const routes = {
  compose: '/',
  inbox: '/inbox',
  // sign: '/sign',
  // decrypt: '/decrypt',
  // records: '/records',
  // transfer: '/transfer',
  // execute: '/execute',
  // deploy: '/deploy',
  // dmail: '/dmail',
};

export default routes;
